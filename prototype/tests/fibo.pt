/* function fibFastRec(n) {
    function fib(prvprv, prv, c) {
        if (c < 1){
            return prvprv
        } else {
            return fib(prv, prvprv + prv, c - 1)
        }
        return fib(0, 1, n)
    }
}

for (i = 1; i < 31; i++) {
    print(fibFastRec(i))
}
 */
a.print(1)
